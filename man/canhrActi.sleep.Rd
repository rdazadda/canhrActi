% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_sleep.R
\name{canhrActi.sleep}
\alias{canhrActi.sleep}
\title{Batch Sleep Analysis for ActiGraph Data}
\usage{
canhrActi.sleep(
  agd_file_path,
  sleep_algorithm = c("cole.kripke", "sadeh"),
  apply_rescoring = TRUE,
  detect_sleep_period = TRUE,
  bedtime_start = 5,
  wake_time_end = 10,
  min_sleep_period = 160,
  max_sleep_period = 1440,
  min_nonzero_epochs = 15,
  export = TRUE,
  output_dir = "sleep_analysis_output",
  parallel = NULL,
  n_cores = NULL,
  verbose = TRUE,
  memory_efficient = TRUE
)
}
\arguments{
\item{agd_file_path}{Single file, vector of files, or folder path}

\item{sleep_algorithm}{"cole.kripke" (adults) or "sadeh" (children/adolescents)}

\item{apply_rescoring}{Apply Webster's rescoring rules? (default: TRUE)}

\item{detect_sleep_period}{Detect sleep periods? (default: TRUE)}

\item{bedtime_start}{Consecutive sleep epochs for bedtime (default: 5)}

\item{wake_time_end}{Consecutive wake epochs for wake time (default: 10)}

\item{min_sleep_period}{Minimum sleep period in minutes (default: 160)}

\item{max_sleep_period}{Maximum sleep period in minutes (default: 1440)}

\item{min_nonzero_epochs}{Minimum non-zero epochs (default: 15)}

\item{export}{Export results to CSV? (default: TRUE)}

\item{output_dir}{Output directory (default: "sleep_analysis_output")}

\item{parallel}{Use parallel processing? (default: TRUE for >4 files)}

\item{n_cores}{Number of CPU cores (default: auto-detect, max 8)}

\item{verbose}{Show detailed progress? (default: TRUE)}

\item{memory_efficient}{Optimize memory for large files? (default: TRUE)}
}
\value{
List with results, summary, and parameters
}
\description{
Performs sleep analysis on ActiGraph files with parallel processing support.
Includes memory optimization and detailed progress tracking.
}
\examples{
\dontrun{
# Single file
results <- canhrActi.sleep("participant.agd")

# Parallel batch processing
results <- canhrActi.sleep("C:/Sleep Data", parallel = TRUE, n_cores = 4)

# View summary
print(results$summary)
}

}
