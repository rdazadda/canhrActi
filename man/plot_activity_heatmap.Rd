% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization_advanced.R
\name{plot_activity_heatmap}
\alias{plot_activity_heatmap}
\title{Enhanced Activity Heatmap Visualization}
\usage{
plot_activity_heatmap(
  data,
  timestamp_col = "timestamp",
  counts_col = "axis1",
  metric = NULL,
  aggregate_func = mean,
  color_palette = "viridis",
  show_values = FALSE,
  normalize = FALSE,
  show_weekends = TRUE,
  show_sleep_window = TRUE,
  sleep_periods = NULL,
  show_summary = FALSE,
  title = "Activity Heatmap"
)
}
\arguments{
\item{data}{Data frame with timestamp and counts columns}

\item{timestamp_col}{Name of timestamp column}

\item{counts_col}{Name of counts column (default: "axis1")}

\item{metric}{Alternative name for counts_col parameter (default: NULL)}

\item{aggregate_func}{Function to aggregate counts (default: mean)}

\item{color_palette}{Color palette: "viridis" (default), "default", "plasma",
"magma", "inferno", "cividis", or custom vector}

\item{show_values}{Logical. Show values in cells? (default: FALSE)}

\item{normalize}{Logical. Normalize values to 0-1 range? (default: FALSE)}

\item{show_weekends}{Logical. Add weekend row markers? (default: TRUE)}

\item{show_sleep_window}{Logical. Mark typical sleep window (22:00-06:00)? (default: TRUE)}

\item{sleep_periods}{Optional data frame with sleep periods for overlay}

\item{show_summary}{Logical. Show daily summary on right margin? (default: FALSE)}

\item{title}{Character. Plot title (default: "Activity Heatmap")}
}
\value{
A ggplot2 heatmap object
}
\description{
Creates a publication-quality heatmap showing activity intensity across days
and hours with colorblind-safe palettes, weekend highlighting, sleep overlay,
and summary statistics panel.
}
\details{
The heatmap provides a comprehensive view of activity patterns:
\itemize{
\item Rows = days, Columns = hours
\item Color intensity = activity level
\item Weekend rows optionally highlighted
\item Sleep window optionally shaded
\item Colorblind-safe palette options via viridis
}
}
\examples{
\dontrun{
results <- canhrActi("participant.agd")

# Basic heatmap
plot_activity_heatmap(results$epoch_data)

# Colorblind-safe with weekend markers
plot_activity_heatmap(results$epoch_data,
                      color_palette = "viridis",
                      show_weekends = TRUE)

# With sleep window overlay
plot_activity_heatmap(results$epoch_data,
                      show_sleep_window = TRUE)
}

}
