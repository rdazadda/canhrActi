% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sleep_analysis.R
\name{sleep.cole.kripke}
\alias{sleep.cole.kripke}
\title{Cole-Kripke Sleep Scoring Algorithm}
\usage{
sleep.cole.kripke(counts, apply_rescoring = TRUE)
}
\arguments{
\item{counts}{Numeric vector of activity counts (from axis1)}

\item{apply_rescoring}{Logical. Apply Webster's rescoring rules? (default: TRUE)}
}
\value{
Character vector of sleep states: "S" (sleep) or "W" (wake)
}
\description{
Implements the Cole-Kripke sleep/wake scoring algorithm for adults (ages 35-65).
}
\details{
Uses 7-epoch window (4 previous + current + 2 next) with weighted coefficients:
D = 0.001 * (106\emph{P4 + 54}P3 + 58\emph{P2 + 76}P1 + 230\emph{C + 74}N1 + 67*N2)

Counts are divided by 100 and capped at 300.
Classification: D < 1 = Sleep, D >= 1 = Wake

Webster's Rescoring (if enabled):
\enumerate{
\item After >= 4 min wake, next 1 min sleep -> wake
\item After >= 10 min wake, next 3 min sleep -> wake
\item After >= 15 min wake, next 4 min sleep -> wake
\item <= 6 min sleep surrounded by >= 15 min wake -> wake
\item <= 10 min sleep surrounded by >= 20 min wake -> wake
}
}
\examples{
\dontrun{
agd.data <- read.agd("participant.agd")
counts.data <- agd.counts(agd.data)
sleep.wake <- sleep.cole.kripke(counts.data$axis1)
table(sleep.wake)
}

}
\references{
Cole RJ, Kripke DF, Gruen W, Mullaney DJ, Gillin JC (1992).
Automatic sleep/wake identification from wrist activity.
Sleep, 15(5):461-469.

ActiGraph documentation:
https://actigraphcorp.my.site.com/support/s/article/Where-can-I-find-documentation-for-the-Sadeh-and-Cole-Kripke-algorithms
}
