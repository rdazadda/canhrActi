% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization_advanced.R
\name{plot_daily_timeline}
\alias{plot_daily_timeline}
\title{Multi-Day Activity Timeline Plot (ActiLife Style)}
\usage{
plot_daily_timeline(
  data,
  timestamp_col = "timestamp",
  show_axes = c("axis1"),
  show_cutpoints = TRUE,
  cutpoints = c(sedentary = 100, light = 1952, moderate = 5725, vigorous = 9498),
  show_inclinometer = FALSE,
  inclinometer_col = "inclinometer",
  equal_scales = TRUE,
  max_counts = NULL,
  title = "Daily Activity Timeline",
  color_scheme = NULL
)
}
\arguments{
\item{data}{Data frame with timestamp and activity columns}

\item{timestamp_col}{Name of timestamp column (default: "timestamp")}

\item{show_axes}{Character vector of axes to display. Options: "axis1", "axis2",
"axis3", "vm", "steps", "hr" (default: c("axis1"))}

\item{show_cutpoints}{Logical. Show activity intensity cut-point lines? (default: TRUE)}

\item{cutpoints}{Named numeric vector of cut-points (default: Freedson adult)}

\item{show_inclinometer}{Logical. Show inclinometer posture as background? (default: FALSE)}

\item{inclinometer_col}{Name of inclinometer/posture column}

\item{equal_scales}{Logical. Use same y-axis scale for all days? (default: TRUE)}

\item{max_counts}{Maximum y-axis value (default: auto)}

\item{title}{Plot title (default: "Daily Activity Timeline")}

\item{color_scheme}{Named list of colors for each metric}
}
\value{
A ggplot2 object
}
\description{
Creates a stacked multi-day activity visualization similar to ActiLife's
graphing interface, with each day displayed as a separate row.
}
\details{
This function replicates ActiLife's multi-day graphing view where each day
is displayed as a horizontal timeline stacked vertically. Features include:

\itemize{
\item Multiple metrics on same plot (Axis1, Axis2, Axis3, VM, Steps, HR)
\item Cut-point threshold lines for activity classification
\item Inclinometer posture as background shading
\item Equal or independent y-axis scales per day
\item Professional color scheme matching ActiLife
}
}
\examples{
\dontrun{
results <- canhrActi("participant.agd")

# Basic plot with Axis1 only
plot_daily_timeline(results$epoch_data)

# Multi-axis plot
plot_daily_timeline(results$epoch_data,
                    show_axes = c("axis1", "axis2", "vm"),
                    show_cutpoints = TRUE)

# With inclinometer overlay
plot_daily_timeline(results$epoch_data,
                    show_axes = c("axis1"),
                    show_inclinometer = TRUE,
                    inclinometer_col = "inclinometer")
}

}
