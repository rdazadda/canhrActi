% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_phase.R
\name{circadian.is.multiscale}
\alias{circadian.is.multiscale}
\title{Multi-resolution Interdaily Stability (IS)}
\usage{
circadian.is.multiscale(counts, timestamps, bin_minutes = c(60, 30, 15))
}
\arguments{
\item{counts}{Numeric vector of epoch-level activity.}

\item{timestamps}{POSIXct timestamps (one per epoch).}

\item{bin_minutes}{Integer vector of bin widths in minutes (default
\code{c(60, 30, 15)}).}
}
\value{
Data frame with columns \code{bin_minutes} and \code{IS}.
}
\description{
Computes interdaily stability at several within-day bin resolutions. IS is
classically reported at 60-min bins, but finer bins (30/15 min) capture
higher-frequency day-to-day regularity. For each bin width,
\code{IS = (N * sum_h (xbar_h - xbar)^2) / (p * sum_i (x_i - xbar)^2)} where
\code{p} is bins-per-day, \code{xbar_h} the mean for bin-of-day \code{h},
and \code{N} the total number of bins.
}
\references{
Witting W, et al. (1990). Biol Psychiatry 27(6):563-572.
}
